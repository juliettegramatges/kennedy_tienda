services:
  - type: web
    name: kennedy-tienda
    env: python
    buildCommand: 
      - pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kennedy-db
          property: connectionString

databases:
  - name: kennedy-db
    plan: free
